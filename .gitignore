# — dependencies —
node_modules/
.pnpm-store/

# — next.js —
.next/
out/
.vercel/

# — go —
api/tmp/
api/bin/
*.exe

# — flutter —
apps/mobile/.dart_tool/
apps/mobile/build/
apps/mobile/.packages
apps/mobile/pubspec.lock

# — env —
.env
.env.local
.env.*.local

# — ide —
.vscode/
.idea/
*.swp
*.swo

# — os —
.DS_Store
Thumbs.db

# — docker —
docker/volumes/

# — turbo —
.turbo/

# — logs —
*.log
